import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#6200EA'
    },
    secondary: {
      main: '#18FFFF'
    },
    text: {
      primary: '#fff'
    }
  }
})

export default theme
